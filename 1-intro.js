// An environment to run javascript outside of browser 
//that’s built on top of chromes v8 engine. 
// Node.js is an open-source, cross-platform, 
// server-side JavaScript runtime environment

// Node.js has no DOM, no window; however it is based 
// on versions, and is written in commonJS.

const amount = 9

if (amount < 10) {
    console.log('small number')
}

else{
    console.log('large number')
}

console.log(`hey it's my first node app!!!`)